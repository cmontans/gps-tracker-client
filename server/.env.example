# PostgreSQL Database URL
# Format: postgresql://username:password@host:port/database
#
# Koyeb Managed PostgreSQL:
#   Get the connection string from Koyeb dashboard → Databases → your DB → Overview
#   DATABASE_URL=postgresql://koyeb-adm:<password>@<host>.koyeb.app:5432/<db>?sslmode=require
#
# Other providers (Neon, Railway, Supabase):
#   See KOYEB_DEPLOYMENT.md for connection string formats
#
DATABASE_URL=postgresql://user:password@localhost:5432/gps_tracker

# Node environment (development/production)
NODE_ENV=development
